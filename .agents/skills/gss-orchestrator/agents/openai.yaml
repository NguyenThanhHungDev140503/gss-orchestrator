interface:
  display_name: "Gss Orchestrator"
  short_description: "Repo-specific patterns and workflows for gss-orchestrator"
  default_prompt: "Use the gss-orchestrator repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true